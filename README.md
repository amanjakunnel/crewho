# Crewho - Networking App (Fullstack)
Collaborative project. My contributions: [list, e.g., frontend screens, backend models]. Credits: [team links/emails].

## Frontend (React/Next)
cd frontend && npm install && npm run dev

## Backend (Node/Express)
cd backend && npm install && npm start
